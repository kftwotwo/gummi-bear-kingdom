<%= link_to "New product", new_product_path %>

<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, product_path(product) %> <%= link_to "Edit", edit_product_path(product) %> | <%= link_to "Delete", product_path(product), :data => {:confirm => "You sure?", :method => "delete" }%></li>
  <% end %>
</ul>
